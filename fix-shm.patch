diff --git a/app/page.tsx b/app/page.tsx
index 1111111..2222222 100644
--- a/app/page.tsx
+++ b/app/page.tsx
@@ -1,6 +1,6 @@
 "use client";
 import RewardsChart from "./components/RewardsChart";
 import { useEffect, useState } from "react";
 import TotalRewards from "./components/TotalRewards";
@@ -179,7 +179,7 @@ export default function Page() {
                 {delegations
                   ? delegations.total_stake.toLocaleString()
                   : "—"}{" "}
-                ASHM
+                SHM
               </div>
 
               {delegations && delegations.diff > 0 && (
                 <div className="text-xs text-green-400 mt-1">
-                  +{delegations.diff} ASHM
+                  +{delegations.diff} SHM
                 </div>
               )}
             </div>
@@ -220,7 +220,7 @@ export default function Page() {
       <div key={i} className="text-xs text-gray-300">
         <span className="text-green-400">
-          +{d.delta.toLocaleString()} ASHM
+          +{d.delta.toLocaleString()} SHM
         </span>{" "}
         from{" "}
         <span className="font-mono text-gray-400">
           {d.delegator.slice(0, 10)}…
@@ -250,7 +250,7 @@ export default function Page() {
         <span className="text-green-400 font-semibold">
-          +{d.delta} ASHM
+          +{d.delta} SHM
         </span>
       </div>
     ))}
@@ -278,18 +278,18 @@ export default function Page() {
                 <span className="text-2xl">🛢️</span>
                 <div className="text-3xl font-bold text-green-400">
-                  +{rewards.rewards_24h.toFixed(4)} ASHM
+                  +{rewards.rewards_24h.toFixed(4)} SHM
                 </div>
 
-               <div className="text-sm text-gray-400 mt-1">
-                 (~ ${rewards.rewards_usdt?.toFixed(2) || "0.00"})
-               </div>
+                <div className="text-sm text-gray-400 mt-1">
+                  (~ ${rewards.rewards_usdt?.toFixed(2) || "0.00"})
+                </div>
 
-               <div className="text-lg text-gray-300">
-                 (~ $
-                 {(rewards.rewards_24h * rewards.price_usdt).toFixed(2)}
-                 )
-               </div>
+                <div className="text-lg text-gray-300">
+                  (~ ${(rewards.rewards_24h * rewards.price_usdt).toFixed(2)})
+                </div>
               </div>
               <div className="text-xs text-gray-500 mt-3">
                 Updated: {new Date(rewards.updated).toLocaleString()}
diff --git a/app/components/RewardsChart.tsx b/app/components/RewardsChart.tsx
index 3333333..4444444 100644
--- a/app/components/RewardsChart.tsx
+++ b/app/components/RewardsChart.tsx
@@ -1,6 +1,6 @@
 "use client";
 
 import { useEffect, useState } from "react";
 
 type Point = { date: string; rewards: number };
@@ -60,7 +60,7 @@ export default function RewardsChart() {
   // ... (твой текущий рендер графика)
   // Ниже — типовые замены подписей (оставь твою библиотеку/верстку как есть)
 
-  // label/unit: "ASHM"
+  // label/unit: "SHM"
 
   return (
     <div>
@@ -70,7 +70,7 @@ export default function RewardsChart() {
-        <div className="text-sm text-gray-400">Rewards (daily) · ASHM</div>
+        <div className="text-sm text-gray-400">Rewards (daily) · SHM</div>
       </div>
 
       {/* Если есть tooltip/ось/легенда с "ASHM" — поменяй на "SHM" */}
@@ -90,7 +90,7 @@ export default function RewardsChart() {
-      {/* Example tooltip: {v} ASHM */}
+      {/* Example tooltip: {v} SHM */}
     </div>
   );
 }
diff --git a/app/api/rewards/history/route.ts b/app/api/rewards/history/route.ts
index 5555555..6666666 100644
--- a/app/api/rewards/history/route.ts
+++ b/app/api/rewards/history/route.ts
@@ -18,7 +18,10 @@ export async function GET() {
     const lines = text.trim().split("\n").slice(1);
 
     // парсим как cumulative total
     const totals = lines.map((line) => {
       const [date, raw] = line.split(",");
@@ -26,18 +29,24 @@ export async function GET() {
       const num = parseFloat(
         raw.trim().startsWith(".") ? "0" + raw.trim() : raw.trim()
       );
 
-      // convert atto → ASHM
-      return {
-        date,
-        total: num / 1e18,
-      };
+      // В CSV у тебя встречались и atto (огромные), и уже человекочитаемые значения.
+      // Для графика хотим ВСЕГДА человекочитаемое значение (SHM).
+      const totalShm = num > 1e10 ? num / 1e18 : num;
+
+      return { date, total: totalShm };
     });
 
     // превращаем в приросты по дням
     const data = [];
 
     for (let i = 1; i < totals.length; i++) {
       const diff = totals[i].total - totals[i - 1].total;
diff --git a/app/components/TotalRewards.tsx b/app/components/TotalRewards.tsx
index 7777777..8888888 100644
--- a/app/components/TotalRewards.tsx
+++ b/app/components/TotalRewards.tsx
@@ -1,6 +1,6 @@
 "use client";
 
 import { useEffect, useState } from "react";
 
 type RewardsApi = {
-  total_rewards: number;
+  total_rewards?: number;
+  total_rewards_shm?: number;
   total_usdt?: number;
   updated: string;
   price_usdt?: number;
   error?: string;
   reason?: string;
 };
@@ -48,13 +48,18 @@ export default function TotalRewards() {
 
   const formattedDate = new Date(data.updated).toLocaleString();
 
+  const totalShm = Number(
+    (data as any).total_rewards_shm ?? (data as any).total_rewards ?? 0
+  );
+
   return (
     <div className="space-y-1">
       <div className="text-3xl font-semibold">
-        {Number(data.total_rewards_shm ?? data.total_rewards ?? 0).toFixed(4)} SHM
+        {Number.isFinite(totalShm) ? totalShm.toFixed(4) : "0.0000"} SHM
         {usd && (
           <span className="ml-2 text-gray-400 text-lg">
             (~${usd})
           </span>
         )}
       </div>
